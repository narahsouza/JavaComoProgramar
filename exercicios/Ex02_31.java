// Exercício 2.31: Ex02_31.java
// Tabela de quadrados e cubos

public class Ex02_31 {

    public static void main (String [] args) {

        int number;

        System.out.println("number square cube");
        
        number = 0;
        System.out.printf("%d      %d      %d%n", number, number * number, number * number * number);

        number = 1;
        System.out.printf("%d      %d      %d%n", number, number * number, number * number * number);

        number = 2;
        System.out.printf("%d      %d      %d%n", number, number * number, number * number * number);

        number = 3;
        System.out.printf("%d      %d      %d%n", number, number * number, number * number * number);

        number = 4;
        System.out.printf("%d      %d     %d%n", number, number * number, number * number * number);

        number = 5;
        System.out.printf("%d      %d     %d%n", number, number * number, number * number * number);

        number = 6;
        System.out.printf("%d      %d     %d%n", number, number * number, number * number * number);

        number = 7;
        System.out.printf("%d      %d     %d%n", number, number * number, number * number * number);

        number = 8;
        System.out.printf("%d      %d     %d%n", number, number * number, number * number * number);

        number = 9;
        System.out.printf("%d      %d     %d%n", number, number * number, number * number * number);

        number = 10;
        System.out.printf("%d     %d    %d%n", number, number * number, number * number * number);
    }
}